import { HuskyConfigBase } from '../base';
/**
 * Implementing all relevant Husky configuration for the CapService project.
 */
export declare class HuskyConfigCapService extends HuskyConfigBase {
    protected get configFile(): Record<string, string[]>;
}
