name: Enforce default patch version

on:
  pull_request:
    types: [opened]

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - name: Enforce labels
        uses: dxfrontier/gh-action-enforce-version@main
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
