import { GitBaseConfig } from '../base';

/**
 * Implementing all relevant GIT configuration for the CapService project.
 * @extends GitBaseConfig
 */
export class GitConfigCapService extends GitBaseConfig { }