import { DevContainerBaseConfig } from '../base';

/**
 * Implementing all relevant DevContainer configuration for the CapService project.
 * @extends DevContainerBaseConfig
 */
export class DevContainerConfigCapService extends DevContainerBaseConfig {}
