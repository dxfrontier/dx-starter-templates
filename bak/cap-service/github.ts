import { GitHubBaseConfig } from '../base';

/**
 * Implementing all relevant GitHub configuration for the CapService project.
 * @extends GitHubBaseConfig
 */
export class GitHubConfigCapService extends GitHubBaseConfig {}
