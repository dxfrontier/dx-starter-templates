import { HuskyBaseConfig } from '../base';

/**
 * Implementing all relevant Husky configuration for the CapService project.
 * @extends HuskyBaseConfig
 */
export class HuskyConfigCapService extends HuskyBaseConfig { }