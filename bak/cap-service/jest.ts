import { JestBaseConfig } from '../base';

/**
 * Implementing all relevant Jest configuration for the CapService project.
 * @extends JestBaseConfig
 */
export class JestConfigCapService extends JestBaseConfig {}
