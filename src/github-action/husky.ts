import { HuskyBaseConfig } from '../base';

/**
 * Implementing all relevant Husky configuration for the GithubAction project.
 * @extends HuskyBaseConfig
 */
export class HuskyConfigGithubAction extends HuskyBaseConfig { }